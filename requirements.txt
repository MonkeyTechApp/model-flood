tensorflow>=2.10.0  # Use CPU-only to reduce size
flask==3.0.0            # Minimal web framework
gunicorn==21.2.0        # Lightweight server
numpy==1.24.4           # Pinned to avoid bloat
scikit-learn
joblib
pandas